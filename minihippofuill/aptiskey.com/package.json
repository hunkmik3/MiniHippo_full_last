{
  "name": "aptiskey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:vstep": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi; vercel dev --listen ${PORT:-3005}"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1048.0",
    "@aws-sdk/s3-request-presigner": "^3.1048.0"
  }
}
