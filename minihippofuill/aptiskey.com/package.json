{
  "name": "aptiskey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi; vercel dev --listen 3100",
    "dev:vstep": "if [ -f .env.local ]; then set -a; . ./.env.local; set +a; fi; vercel dev --listen 3100"
  }
}
